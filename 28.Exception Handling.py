"""
Error & Exception:
==================
Error:
======
It is excutes compile time error.

it is occurs due to improper usage of program statements.
syntax error.

syntax error:
=============
when we are not maintain to structure of programming correctly

print()#correct
print)(#wrong
if a>b:#correct
if a>b #wrong


exception:
==========
we can write a program is syntactically correct
but we are do mistakes for inputs

Run time error

NameError:
==========
a=10
#b=20
c=a+b

valueError:
============
num=int(input("Enter data : "))
Enter data : "abi"#only store number

ZeroDivisionError:
==================

a=10
b=0
c=a/b# dividend should be above zero

ModuleNotFoundError:
====================
if modules are not available


AttributeError:
===============
int can store multiple values
it can't that type function

a=10
a.add(2)

AssertionError:
===============

mark=int(input("Enter mark : "))
assert mark>=0 and mark<=100#if the condition it will display AssertionError
print("mark : ",mark)

KeyboardInterrupt:
==================
when we press ctrl+c that the time raised this type error

IndexError:
===========
when we provide not available position that the time raised these error
l=[1,2,3]
print(l[3])

"""
